%h1 Listing projects

%table
  %tr
    %th Naam
    %th
    %th
    %th

  - @projects.each do |project|
    %tr
      %td= project.name
      %td= link_to 'Show', city_project_path(project)
      %td= link_to 'Edit', edit_city_project_path(project)
      %td= link_to 'Destroy', city_project_path(project), :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Project', new_city_project_path
