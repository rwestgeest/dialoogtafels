= form_for @city_locations_publication do |f|
  -if @city_locations_publication.errors.any?
    #error_explanation
      %h2= "#{pluralize(@city_locations_publication.errors.count, "error")} prohibited this city_locations_publication from being saved:"
      %ul
        - @city_locations_publication.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
