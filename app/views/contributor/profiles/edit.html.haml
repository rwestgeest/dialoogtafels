- title "Mijn profiel"
= form_for @person, :url => contributor_profile_path  do |f|
  = errors_for @person

  .input-container
    .label= f.label :name
    .input= f.text_field :name

  .input-container
    .label= f.label :email
    .input= f.text_field :email

  .input-container
    .label= f.label :telephone
    .input= f.text_field :telephone

  - @profile_fields.each do |profile_field|
    .input-container
      .label= f.label profile_field.field_name_with_prefix, profile_field.label
      .input= f.text_field profile_field.field_name_with_prefix

  .actions
    = f.submit

