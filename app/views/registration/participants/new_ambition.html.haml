- title "Aanmelden als deelnemer"

%p
  Als je direct bij een locatie wilt aanmelden 
  = link_to 'kies dan hier een locatie', locations_path

= form_for @person, :url => registration_participants_path  do |f|
  = errors_for @person
  .dialog-container
    .input-container
      .label= f.label :email
      .input= f.text_field :email

    .input-container
      .label= f.label :name
      .input= f.text_field :name

    .input-container
      .label= f.label :telephone
      .input= f.text_field :telephone

  - unless @profile_fields.empty?
    .dialog-container
      - @profile_fields.each do |profile_field|
        .input-container
          .label= f.label profile_field.field_name_with_prefix, profile_field.label
          .input= profile_field.render_field_on(f)
      .clear
  .dialog-container
    = training_registration_tags @training_types, @person, 'ik doe niet mee aan'
    .clear
  .dialog-container
    = render partial: 'shared/captcha'
    .clear

  .actions
    = link_to "terug", locations_path
    = f.submit

