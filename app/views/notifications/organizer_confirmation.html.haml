= markup(@message)

%p 
  Je logt in met je email en wachtwoord. Klik 
  = link_to "hier", account_response_session_url(@applicant.account.authentication_token, :host => @tenant.host)
  om je account te activeren.

= render :partial => 'applicant_details'

